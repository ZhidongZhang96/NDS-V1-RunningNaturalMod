__pycache__/
.vscode/
.claude/
.idea/
.DS_Store

# Per-container session dumps (large, ~0.1-0.7 GB each; fetched on demand via
# scripts/download_container.py, consumed by load_containers — never committed)
data/container_*.npz

# AllenSDK downloads / cache (large; fetched on demand, not tracked)
boc/ophys_experiment_data/
boc/ophys_experiment_events/
boc/cell_specimens.json
boc/experiment_containers.json
boc/ophys_experiments.json
